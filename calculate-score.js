function calculateFinalScore(obj) {
    if("obj")
        return "Invalid Input"
    const finalScore = obj.name + obj.testScore + obj.schoolGrade + obj.isFFamily;
        finalScore = + 20
    return finalScore
    
}



